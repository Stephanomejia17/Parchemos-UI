{
  "name": "parchemos",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @parchemos/web dev",
    "dev:customer": "pnpm --filter @parchemos/web dev",
    "build": "pnpm -r --if-present build",
    "build:customer": "pnpm --filter @parchemos/web build"
  },
  "devDependencies": {
    "pnpm": "9.15.0"
  }
}
